rootProject.name = "i-eat-what-backend"
include 'src:models',
        'src:api',
        'src:event-manager',
        'src:db-manager',
        'src:workers'

// need to exclude for prod push
include 'src:client'

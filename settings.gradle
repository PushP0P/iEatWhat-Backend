rootProject.name = "i-eat-what-backend"
include 'src:models',
        'src:api',
        'src:event-manager',
        'src:db-manager',
        'src:workers',
        'src:search-manager',
        'src:user-manager'

// need to exclude for prod push
//include 'src:client'
